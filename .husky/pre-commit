# Quick quality checks on staged files
npx lint-staged

# Ensure project builds successfully
npm run build

# Keep ADR documentation up to date
npx adrx build

echo "✅ Pre-commit checks passed! Remember to run 'npm test' before pushing."
