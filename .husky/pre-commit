# Quick quality checks on staged files
npx lint-staged

# Ensure project builds successfully
npm run build

echo "✅ Pre-commit checks passed!"
echo "💡 ADR build skipped to avoid commit loops."
echo "🔄 Run 'npm run adr:update' manually when ADR files change."
